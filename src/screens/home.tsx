import { Text, View } from "react-native";
import { Input } from "@/components";
const Home: React.FC = () => {
  return (
    <View
      style={{
        width: "100%",
        padding: 20,
      }}
    >
      <Input />
    </View>
  );
};

export default Home;
