import { Text, View } from "react-native";
import { Input } from "@/components";
const Home: React.FC = () => {
  return (
    <View
      style={{
        width: "100%",
        padding: 20,
      }}
    ></View>
  );
};

export default Home;
